name: Create Jira Sub-task under Task
on:
  issues:
    types: [opened]

jobs:
  create-subtask-under-task:
    runs-on: ubuntu-latest
    steps:
      - uses: atlassian/gajira-login@v3
        env:
          JIRA_BASE_URL: ${{ secrets.JIRA_BASE_URL }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
          JIRA_USER_EMAIL: ${{ secrets.JIRA_USER_EMAIL }}

      - uses: actions/checkout@v4
        with: { ref: main }

      - id: issue-parser
        uses: stefanbuck/github-issue-parser@v3
        with:
          template-path: .github/ISSUE_TEMPLATE/issue-form.yml

      - name: Guard - parentKey required
        if: ${{ steps.issue-parser.outputs.issueparser_parentKey == '' }}
        run: |
          echo "No parent key provided. Skip."
          exit 0

      - id: md2jira
        uses: peter-evans/jira2md@v1
        with:
          input-text: |
            ### Github Issue Link
            - ${{ github.event.issue.html_url }}

            ${{ github.event.issue.body }}
          mode: md2jira

      - name: Create Sub-task under Task (optimistic)
        id: create-subtask
        uses: atlassian/gajira-create@v3
        continue-on-error: true
        with:
          project: SGK
          issuetype: 하위 작업
          summary: '${{ github.event.issue.title }}'
          description: '${{ steps.md2jira.outputs.output-text }}'
          fields: |
            {
              "parent": {
                "key": "${{ steps.issue-parser.outputs.issueparser_parentKey }}"
              }
            }

      - name: Skip when parent is not Task
        if: ${{ steps.create-subtask.outcome != 'success' }}
        run: |
          echo "Parent is not Task or creation failed. Skip."
          exit 0

      - uses: actions/checkout@v4
        if: ${{ steps.create-subtask.outcome == 'success' }}
        with: { ref: dev }

      - name: Create branch with Ticket number
        if: ${{ steps.create-subtask.outcome == 'success' }}
        run: |
          ISSUE="${{ steps.create-subtask.outputs.issue }}"
          TITLE="${{ steps.issue-parser.outputs.issueparser_branch }}"
          BRANCH="${ISSUE}-$(echo "$TITLE" | sed 's/ /-/g')"
          git checkout -b "$BRANCH"
          git push origin "$BRANCH"

      - name: Update GitHub issue title
        if: ${{ steps.create-subtask.outcome == 'success' }}
        uses: actions-cool/issues-helper@v3
        with:
          actions: 'update-issue'
          token: ${{ secrets.GITHUB_TOKEN }}
          title: '[${{ steps.create-subtask.outputs.issue }}] ${{ github.event.issue.title }}'

      - name: Comment with Jira link
        if: ${{ steps.create-subtask.outcome == 'success' }}
        uses: actions-cool/issues-helper@v3
        with:
          actions: 'create-comment'
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.issue.number }}
          body: 'Jira Issue Created: [${{ steps.create-subtask.outputs.issue }}](${{ secrets.JIRA_BASE_URL }}/browse/${{ steps.create-subtask.outputs.issue }})'
